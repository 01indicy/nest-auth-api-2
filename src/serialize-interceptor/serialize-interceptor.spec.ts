import { SerializeInterceptor } from './serialize-interceptor.service';

describe('SerializeInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new SerializeInterceptor()).toBeDefined();
  });
});
